BREAKING: `UnverifiedClientChannel` is not public anymore
BREAKING: `ClientInitiatorHandshake::connect()` returns a Box<dyn VerifiableChannel>
BREAKING: `RelayInitiatorHandshake::connect()` returns a Box<dyn VerifiableChannel>
