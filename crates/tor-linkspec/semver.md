BREAKING: `HasAddrs::addrs` now returns `impl Iterator` rather than a borrowed slice.
BREAKING: `HasAddrs` is no longer dyn compatible.
BREAKING: `DirectChanMethodsHelper` is no longer dyn compatible.
